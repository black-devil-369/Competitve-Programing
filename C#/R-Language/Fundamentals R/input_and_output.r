# method to take input from user in R-Language
a = readline();
# method to convert a given variable into integers
a = as.integer(a);
# method is used to print output on windows
print(a);

b = readline(prompt = "Enter your Number \n");
b = as.integer(b);
print(b);
# another method to take a input in R-Language
# this method take a input in the form of vector and list
x = scan();
print(x);
# this method is used to take double input by using scan method
y = scan(what = double());
print(y);